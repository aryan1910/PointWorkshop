#include <iostream>
using namespace std;
//Counting vowels in a string
int main() {
	int a,b;
	cin>>a>>b;
	if(a>b)
		cout<<"a is greater than b";
	else if(a<b)
		cout<<"a is lesser than b";
	else
		cout<<"a is equal to b";
	return 0;
}
