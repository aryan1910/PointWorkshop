#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cin>>n;
	int arr[n];
	for(int i=0;i<n;i++)
		cin>>arr[i];
	int num;
	cin>>num;
	vector<int> v;
	for(int i=0;i<n;i++){
		if(arr[i]==num){
			v.push_back(i);
		}
	}
	for(int i=0;i<v.size();i++)
		cout<<v[i]<<" ";
	return 0;
}
